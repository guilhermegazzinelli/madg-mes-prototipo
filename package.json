{
  "name": "madg-mes-prototipo",
  "version": "0.1.6",
  "private": true,
  "description": "MADG MES — Sistema de monitoramento OEE para indústria",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
